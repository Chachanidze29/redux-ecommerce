import styled from "styled-components";

const CartLinkContainer = styled.div`
    position: relative;
  
    p {
        position: absolute;
        top: -20px;
        right: -20px;
        color: white;
    }
`

export default CartLinkContainer;